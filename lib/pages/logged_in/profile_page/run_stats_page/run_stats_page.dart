import 'package:flutter/material.dart';

class RunStatsPage extends StatelessWidget {
  const RunStatsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text(
        "STATS HERE",
        style: TextStyle(fontSize: 20),
      )),
    );
  }
}
